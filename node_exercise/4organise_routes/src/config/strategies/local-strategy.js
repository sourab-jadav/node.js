// const passport = require('passport')

// const { Strategy } = require('passport-local')


// module.exports= function localStrategy(){
//     passport.use(new Strategy({
//         //giving the names of the fields when the form is submitted
//         usernameField:'username',
//         passwordField:'password'
//     },(username,password,done)=>{//we send thoose username and password to this function 
//         const user = {username,password,'name':'jonathan'}
//         done(null,user)
//     }))
// }
// //now go to the authrouter and handle the post to signup 
